import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liqpass.app',
  appName: 'Liqpass',
  webDir: 'dist',
  android: {
    backgroundColor: '#0a0a0a',
  },
};

export default config;
